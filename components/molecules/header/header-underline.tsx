import { Box } from '@material-ui/core';
import styled from 'styled-components';

export const HeaderUnderline = styled(Box)`
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1px;
  background-color: white;
  opacity: 0.2;
`;
